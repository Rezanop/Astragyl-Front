import React, { FC } from 'react';
import styled from 'styled-components';

const Wrapper = styled.div``;

const Home: FC = () => {
  return (
    <Wrapper>
      <p>Home Page</p>
    </Wrapper>
  );
};

export default Home;
